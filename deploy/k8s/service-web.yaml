apiVersion: v1
kind: Service
metadata:
  name: touchstone-web
  namespace: touchstone
spec:
  type: ClusterIP
  selector:
    app: touchstone-web
  ports:
    - name: http
      port: 8080
      targetPort: http
